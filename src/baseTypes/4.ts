/*
  Rewrite this in TypeScript using the concept of tuples,
  to ensure that the first element is always a string and the second is a number?
*/

let person: [string, number] = ["Max", 21];

export {};
