/*
  Create a type "Gender" using a union type,
  which can have the values "male" or "female". Create a variable myGender of this type.
*/

type Gender = "male" | "female";

const myGender: Gender;

export {};
